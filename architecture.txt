Order           -> order.h class            -> all the single order related stuff (order id, price, quantity, side, status, type, etc.)
OrderModify     -> orderModify.h class      -> all the order modify related stuff (modify order id, price, quantity, etc.)
Trade           -> trade.h class            -> all the trade related stuff (trade type, price, quantity, etc.)
OrderBook       -> orderbook.h class        -> all the order book related stuff (order book id, price, quantity, etc.)
